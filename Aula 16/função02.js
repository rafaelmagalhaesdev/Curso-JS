function soma(n1=0, n2=0){ //para que a não haja erro podemos fazer o "n1" considerar "0" colocando o n1=0 assim não tornando o valor NaN
    return n1 + n2
}
console.log(soma(2,8)) //Quando não colocamos um valor da um "erro" e com isso considerei colocar um "n1=0" para que isso não ocorra